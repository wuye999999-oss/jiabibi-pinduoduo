{
  "name": "jiabibi-api",
  "version": "9.0.0",
  "description": "Jiabibi API backend — PDD / JD / Taobao active; Douyin pending OAuth integration",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/sandbox.test.js",
    "playwright:install": "npx playwright install chromium"
  },
  "engines": { "node": ">=18 <23" },
  "dependencies": {
    "playwright": "^1.44.0"
  }
}
